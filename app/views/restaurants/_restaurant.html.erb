<figure>
	<figcaption>
	<%= link_to restaurant.name, restaurant_reservations_path(restaurant) %>
	</figcaption>
	<%= link_to image_tag(restaurant.image), restaurant_reservations_path(restaurant) %><br>	
</figure>

